import { ReactNode } from 'react';

/**
 * Login Layout
 * 
 * Layout específico para páginas de autenticação.
 * Remove header/footer e aplica estilo centralizado.
 */
export default function LoginLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
